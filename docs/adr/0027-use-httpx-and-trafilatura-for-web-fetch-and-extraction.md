# Use httpx and trafilatura for Web fetch and extraction

V1 Web Research will use `httpx` for HTTP fetching and `trafilatura` for HTML content extraction. This supports source collection without introducing browser automation, authenticated browsing, anti-scraping workarounds, or large-scale crawling.
